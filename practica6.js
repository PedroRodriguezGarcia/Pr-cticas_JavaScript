// Array
let listaCompra = ["lomo", "chocoflakes", "huevos", "manzanas"];

// Recorrer el array con un for
for (let i = 0; i < listaCompra.length; i++) {
  console.log((i + 1) + " - " + listaCompra[i]);
}
